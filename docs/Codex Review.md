# Codex Review

دفتر متابعة مراجعة المشروع. هذا الملف لا يعني أن أي تعديل على الكود
مسموح تلقائياً: كل مشكلة تُعرض أولاً مع دليل وإصلاح مقترح، ثم يقرر المالك
إن كان يريد تنفيذ الإصلاح.

## كلمات بسيطة

- **جزء مراجعة:** شيء صغير نفحصه في جلسة واحدة، مثل تسجيل دخول الإدارة.
- **نسخة تجريبية (`staging`):** نسخة منفصلة من الموقع للاختبار، لا تمس الموقع
  الحقيقي ولا بياناته.
- **مشكلة P0:** خطر كبير، مثل وصول غير مصرح إلى الإدارة أو تغيير مبلغ فاتورة.
- **مشكلة P1:** شيء أساسي لا يعمل كما يجب.
- **مشكلة P2:** مشكلة مهمة لا تكسر النظام كله.
- **مشكلة P3:** تحسين أو ملاحظة صغيرة.

## قواعد العمل

- جزء واحد فقط في اليوم.
- نبدأ بالتقرير؛ لا نغير الكود أو قاعدة البيانات أو الإعدادات قبل موافقة صريحة.
- أي اختبار يكتب بيانات أو يرسل رسالة يجري على النسخة التجريبية فقط.
- رسائل الاختبار ترسل إلى بريد يملكه المالك فقط، بعنوان يبدأ بـ `[TEST]`.
- لا يوضع أي بريد شخصي أو كلمة مرور أو مفتاح سري في Git أو في هذا الملف.
- لكل نتيجة: النطاق، ما تم فحصه، الدليل، مستوى الخطورة، الإصلاح المقترح، والقرار.

## طريقة العمل المتفق عليها — 20 Sep 2026

تغيّرت الطريقة بعد أن تبيّن أن النسخة التجريبية المنشورة تعطّل المراجعة كلها
بينما هي مطلوبة لجزء صغير منها فقط.

- **نقرأ أولاً، ولا نغيّر شيئاً.** كل جزء يبدأ بقراءة الكود وتقديم تقرير
  بالنتائج. المالك يقرر بعد التقرير ما يُنفَّذ.
- **الإثبات يكون باختبار محلي، لا بنسخة منشورة.** `bun run test` و
  `bun run typecheck` يعطيان حكماً قاطعاً بلا قاعدة بيانات وبلا نشر.
- **أي اختبار جديد يجب أن يُثبت أنه يفشل.** لا يكفي أن ينجح: نكسر الشيء الذي
  يحرسه عمداً ونتأكد أنه صار أحمر، ثم نعيد الكود كما كان. اختبار لا يستطيع
  الفشل لا يحرس شيئاً.
- **النسخة التجريبية المنشورة تبقى مطلوبة، لكن لثلاثة أشياء فقط:** البريد
  الحقيقي، وTurnstile على نطاق حقيقي، ودورة دفع فاتورة كاملة. بقية الأجزاء
  لا تنتظرها.
- **توفير الاستهلاك:** جزء واحد لكل جلسة، ثم جلسة جديدة. الجلسة الطويلة تعيد
  إرسال كل ما سبق مع كل رسالة، وهذا أكبر مصدر للاستهلاك.

## بوابة النسخة التجريبية

قبل اختبار **البريد الحقيقي، أو Turnstile على نطاق حقيقي، أو دورة دفع فاتورة
كاملة**، يجب أن تكون هذه البنود صحيحة. أما قراءة الكود والاختبارات المحلية
فلا تنتظر أياً منها:

- [ ] الرابط هو `staging.yamanwarda.de`.
- [x] الوصول محمي لحساب المالك فقط، قبل صفحة تسجيل الدخول إلى التطبيق.
- [ ] لا تظهر النسخة التجريبية في Google أو في `sitemap.xml`.
- [~] لها Worker وقاعدة بيانات وR2 وأسرار منفصلة عن الإنتاج. أنشئ R2 وقاعدة
  Neon وHyperdrive منفصلون؛ Worker والأسرار ما زالت غير جاهزة.
- [ ] قيم `BASE_URL` و`BETTER_AUTH_URL` ومفاتيح Turnstile تخص `staging` وحدها.
- [ ] يوجد حساب مدير تجريبي وبيانات وهمية فقط.
- [ ] البريد الحقيقي، إن اختبرناه، يصل فقط إلى صندوق بريد يملكه المالك وبعلامة `[TEST]`.

### ما يلزم لإكمالها

- قاعدة Neon جديدة فارغة، لا نسخة من قاعدة الإنتاج ولا اتصال بها.
- Cloudflare Access مفعّلة لتُغلق الموقع قبل تسجيل دخول التطبيق.
- أسرار جديدة مستقلة: جلسات الإدارة، حماية النماذج، وتحديد المعدل. لا يعاد
  استخدام أسرار الإنتاج.
- مفتاح Resend خاص بالاختبار أو سياسة إرسال تمنع أي مستلم غير صندوق بريد المالك؛
  كل رسالة تبدأ بـ`[TEST]`.
- Turnstile جديد خاص بـ`staging.yamanwarda.de`.

## ترتيب المراجعة

| الحالة | الجزء | ما نثبت أنه صحيح |
| --- | --- | --- |
| [~] | 0. الأساس والنسخة التجريبية | العزل، الاختبارات الموجودة، وما هو منشور فعلاً |
| [~] | 1. أمن لوحة الإدارة | الصلاحيات وحالات الرفض: تمّت وثُبتت باختبار. تبقى: انتهاء الجلسة، الخروج، وتغيير كلمة المرور |
| [~] | 2. الفواتير | الحساب والترقيم والتعديل: تمّت وثُبتت. تبقى: مراجعة الـPDF نفسه، ومزايا طلبها المالك (إعدادات البائع من الواجهة، فلتر السنة، الأرشفة) في جلسة بناء منفصلة |
| [ ] | 3. SEO | اللغات الثلاث، العناوين، الروابط الصحيحة، sitemap، ومنع فهرسة الإدارة |
| [ ] | 4. الموبايل والعربية | الشاشات الصغيرة، RTL، النماذج، الجداول، والإتاحة |
| [ ] | 5. تنظيم الكود وReact Query | مسؤولية كل طبقة، cache، تحديث البيانات، وprefetch المفيد |
| [ ] | 6. السرعة والموثوقية | التحميل، أخطاء الخادم، الاختبارات، والنشر |
| [ ] | 7. فحص الإنتاج النهائي | فحص قراءة فقط؛ لا بيانات تجريبية ولا تغييرات |

## قرار الـmodel قبل كل جزء

- الفحص المعتاد: `GPT-6 Astra` مع `medium`.
- الأمن أو المال أو مشكلة صعبة مثبتة: `GPT-6 Astra` مع `xhigh`.
- `max` لا يستخدم إلا إذا بقيت المشكلة بلا جواب بعد الفحص العادي.
- قبل البدء، نراجع `Settings → Usage`. لا نرفع الاشتراك أو نشتري رصيداً قبل أن
  نعرف استهلاك أول جزء حقيقي.

## معلومات نثبتها قبل الحكم

- الـroadmap يضع الفواتير تحت B9 غير المكتمل، بينما قال المالك إن النظام يعمل.
  نثبت واقع الكود والاختبارات قبل وصفه بالمكتمل أو الناقص.
- توجد وثائق قديمة تصف لغة المدونة بطريقة تختلف عن قاعدة اللغات الثلاث الحالية.
  نثبت مصدر الحقيقة قبل فحص SEO أو اقتراح تغيير.

## سجل النتائج

| التاريخ | الجزء | النتيجة | الخطورة | الدليل | القرار |
| --- | --- | --- | --- | --- | --- |
| 19 Sep 2026 | 0. النسخة التجريبية | لا توجد staging مستقلة الآن؛ النشر مضبوط على الإنتاج فقط، والتطوير المحلي قد يستخدم قاعدة بيانات الإنتاج. لا نختبر الإدارة أو الفواتير بطريقة تغيّر البيانات قبل العزل. | P1 | `wrangler.jsonc` يربط فقط `yamanwarda.de` و`www`; GitHub Actions ينشر فقط من `main`; `docs/development.md` يذكر Neon الإنتاج. | نجهز `staging.yamanwarda.de` بموارد منفصلة قبل الاختبارات السلوكية. |
| 19 Sep 2026 | 0. صلاحية Cloudflare | أصبح اتصال Cloudflare الخاص بـCodex يستطيع قراءة Cloudflare Access بعد تفعيل Zero Trust. | — | قراءة `GET /access/apps` نجحت بعد إنشاء الإعداد من لوحة Cloudflare. | نستخدمه للتحقق فقط؛ أي تغيير صلاحيات مستقبلي يتطلب موافقة المالك. |
| 19 Sep 2026 | 0. حماية staging | Cloudflare Access مفعّلة. يوجد تطبيق `Yaman Warda Staging` لعنوان `staging.yamanwarda.de` مع سياسة `Owner only`. | — | Cloudflare API أكدت سياسة `allow` التي تقبل عضو حساب Cloudflare الحالي فقط، ومدة جلسة 24 ساعة. | تحققت البوابة؛ لا ننشر الـWorker قبل اكتمال قاعدة Neon والأسرار. |
| 19 Sep 2026 | 0. ملفات staging | تم إنشاء مخزن ملفات منفصل `yamanwarda-staging-media` في Cloudflare. لا يحتوي بيانات الإنتاج ولا يرتبط بها. | — | استجابة Cloudflare أكدت إنشاء R2 في منطقة EEUR بتاريخ 19 Sep 2026. | يبقى غير مستخدم حتى نربطه فقط بـWorker وأسرار staging. |
| 19 Sep 2026 | 0. إعدادات staging | التطبيق لن يقلع من دون قاعدة منفصلة و`BETTER_AUTH_SECRET` و`BASE_URL` و`BETTER_AUTH_URL` خاصة بـstaging. البريد وTurnstile والاتصال العكسي تحتاج قيماً منفصلة قبل اختبارها. | P1 | `src/shared/env.ts` يوقف التشغيل عند غياب القيم الأربع الأولى؛ `.env.example` يحصي بقية التبعيات الاختيارية. | لا ننشئ Worker أو DNS قبل وجود Neon وأسرار staging؛ ذلك يمنع نشر موقع يبدو سليماً لكن نماذجه أو جلساته مكسورة. |
| 19 Sep 2026 | 0. اتصال Neon | ثُبت Neon MCP الرسمي بطريقة OAuth؛ إنشاء قاعدة staging تم من واجهة Neon لأن الاتصال الآلي لم يظهر بعد داخل Codex. | P2 | Neon CLI لا يملك جلسة محلية حتى الآن، لكن حساب Neon كان مسجلاً في متصفح Codex. | لا نحتاج API key؛ نربط التطبيق بقاعدة staging من دون كشف connection string في المحادثة. |
| 19 Sep 2026 | 0. قاعدة staging | تم إنشاء مشروع Neon مستقل وفارغ باسم `yamanwarda-staging` في Frankfurt، بخدمة Postgres فقط وبدون بيانات إنتاج. | — | واجهة Neon أكدت: مشروع جديد، فرع `production` داخلي له، قاعدة واحدة وcompute واحد، واستهلاك 0 عند الإنشاء. | نستخدم اتصالًا pooled للتطبيق وdirect للمهاجرات فقط؛ لا نقرأ أو ننسخ قاعدة الإنتاج. |
| 20 Sep 2026 | 0. Hyperdrive staging | تم إنشاء `yamanwarda-staging-db` مرتبط بقاعدة Neon staging فقط، وتعطيل cache فيه. | — | Cloudflare أكدت نوع PostgreSQL واسم قاعدة `neondb` ومنطقة Frankfurt؛ صفحة الإعداد تؤكد أن caching معطّل. | يربط به Worker staging لاحقًا، ولا يستخدم Hyperdrive الإنتاج. |
| 19 Sep 2026 | 1. أمن لوحة الإدارة | لا يوجد اختبار مباشر يثبت أن الضيف يتلقى `401` وأن مستخدمًا غير إداري يتلقى `403` من endpoint إداري. الحماية موجودة في الكود، لكن هذا السلوك المهم غير محمي من regression باختبار. | P2 | كل modules الإدارة تستخدم `adminGuard`، لكن البحث في `src/tests/` وجد اختبارات origin فقط، لا اختبارًا لـ`adminGuard` أو `/api/admin/me`. | بعد موافقة المالك: نضيف اختبار endpoint إداري للضيف وللمستخدم غير الإداري، ثم نتحقق منه على staging. |
| 20 Sep 2026 | 1. أمن لوحة الإدارة | الحماية مزدوجة ومستقلة: `adminGuard` مرة على مجموعة `/admin` كلها، ومرة ثانية داخل كل وحدة من الوحدات الثماني. كل طبقة وحدها تكفي لرفض الغريب. | — | كل ملف من `booking/call/content/inbox/invoice/lead/post/project.route.ts` يستدعي `.use(adminGuard)` بنفسه، إضافة إلى `admin.route.ts`. أُثبت عملياً بحذف كل طبقة على حدة. | لا تغيير؛ البنية صحيحة. |
| 20 Sep 2026 | 1. منع رفع الصلاحية | لا يستطيع أحد أن يصنع لنفسه حساب مدير. التسجيل مغلق، وحقل `role` لا يُقبل من جسم الطلب، والدور يُقرأ من قاعدة البيانات مع كل طلب لا من الكوكي. | — | `disableSignUp: true` و`role: { input: false }` في `src/backend/shared/auth.ts`؛ `disableCookieCache: true` في `admin.guard.ts`. | لا تغيير. سحب صفة المدير يسري فوراً، لا بعد انتهاء الكوكي. |
| 20 Sep 2026 | 1. الطرق الجانبية | لا يوجد طريق يلتف حول الحارس. كل دوال الخادم الثلاث عشرة تخدم محتوى منشوراً فقط، ولا واحدة منها تمسّ بيانات الإدارة. | — | بحث `createServerFn` في `src/` أعطى 13 نتيجة، كلها في `published-posts` و`published-projects` و`published-content` واللغة والجلسة. | لا تغيير. |
| 20 Sep 2026 | 1. غياب الاختبار | كانت الحماية كلها بلا اختبار واحد يحرسها: حذف سطر `adminGuard` من أي وحدة كان يفتحها للعامة بصمت من دون أن يفشل شيء. | P2 | 33 ملف اختبار، ولا واحد منها يذكر `adminGuard` أو `/api/admin/me`. | **نُفِّذ.** أُضيف `src/tests/admin-guard.test.ts`: 41 اختباراً يغطي الطبقتين معاً وكلاً منهما وحدها. أُثبت أنه يصير أحمر عند حذف أي من الطبقتين، ثم أُعيد الكود كما كان. |
| 20 Sep 2026 | 1. مفتاح Turnstile | عند غياب `TURNSTILE_SECRET_KEY` في الإنتاج كان التطبيق يقلع ويستعمل النص `missing-production-secret` كأنه مفتاح. Cloudflare ترفضه، فيُقفل الدخول على المالك نفسه برسالة `Security verification failed` لا علاقة لها بالسبب. | P3 | `src/backend/shared/auth.ts:15` سابقاً. لاحظ أن `turnstile.ts:51` كان يعالج الحالة نفسها بشكل صحيح — كان الخطأ في نسخة ثانية من القاعدة. | **نُفِّذ.** حُذفت النسخة الثانية، ويستعمل `auth.ts` الآن `getTurnstileSecret()` نفسها، فيتوقف الإقلاع برسالة `Environment variable TURNSTILE_SECRET_KEY is missing`. 337 اختباراً و`typecheck` نظيفان بعد التغيير. |
| 20 Sep 2026 | 2. بيانات البائع | لا يمكن إصدار أي فاتورة ما دامت بيانات المالك placeholders. هذا سلوك مقصود ويحمي من إرسال فاتورة غير قانونية. البيانات الناقصة ليست أخطاء برمجية بل قائمة انتظار: `street`, `postcode`, `taxNumber`, `bankName`, `iban`, `bic`. | — | `invoice.service.ts:575` يرفض الإصدار ويسمّي كل حقل ناقص. `seller.ts` يضع `smallBusiness: true` فتكون الضريبة صفراً وتُطبع جملة §19. | لا تغيير. يملأها المالك بعد تسجيل النشاط في ألمانيا. |
| 20 Sep 2026 | 2. تناقض الـroadmap | الطرفان على حق. كل ما يذكره B9 مبني فعلاً ما عدا شيئاً واحداً: جدول `engagements` غير موجود. لذلك بقي المربع فارغاً رغم أن نظام الفواتير يعمل. | — | `0016_invoices.sql` ينشئ `clients` و`invoice_numbers` و`invoices` و`invoice_lines` و`payments`. البحث عن `engagement` لا يعطي إلا تفاعلات المدونة. | يقرر المالك لاحقاً إن كان يحتاج `engagements` أصلاً. |
| 20 Sep 2026 | 2. تعديل فاتورة صادرة | كان `updateInvoice` يشغّل ثلاث جمل منفصلة، وواحدة فقط منها تحمل `AND status = 'DRAFT'`. بين الفحص و`writeLines` كان يمكن إصدار الفاتورة من تبويب آخر، فتُحذف بنود مستند مرقّم ويُكتب فوق `total_cents` مبلغ جديد — على ورقة وصلت العميل بالفعل. | P2 | `writeLines` لم يكن يحمل أي شرط حالة، و`updateInvoice` لم يكن داخل transaction، بينما `allocateAndIssue:485` و`correctInvoice:639` يستعملان القفل الصحيح. | **نُفِّذ.** التعديل صار يأخذ قفل `FOR UPDATE` نفسه، فلا يتداخل الاثنان، والتعديل المتأخر يُرفض بجملة تقول: ألغِ الفاتورة بـcredit note وأصدر واحدة مصححة. أُضيف اختباران في `invoice-numbering.test.ts` وأُثبت فشلهما على الكود القديم. |
| 20 Sep 2026 | 2. الحد بين التصحيح والتزوير | السؤال الذي طرحه المالك له جواب واضح في القانون الألماني، والكود يطبقه أصلاً: قبل الرقم تعديل حر، وبعد الرقم لا تغيير أبداً — بل إلغاء بـcredit note ثم إصدار فاتورة مصححة، ويبقى المستندان ظاهرين. | — | `correctInvoice` ينفذ الإلغاء والتصحيح؛ `deleteInvoice` و`updateInvoice` محصوران في المسودة. | لا تغيير. الأرشفة مسموحة لأنها إخفاء من قائمة لا حذف. |
| 20 Sep 2026 | 2. مفتاح IBAN | لا يوجد أي فحص لصحة الـIBAN في المشروع كله. الفحص الحالي يسأل فقط: هل ما زال مكتوباً `TODO`؟ رقم خاطئ واحد يعطي فاتورة تبدو سليمة وQR سليماً، والعميل لا يستطيع الدفع. | P2 | لا نتيجة لـ`mod97` أو أي تحقق مشابه في `src/`. تعليق `seller.ts` نفسه يحذر: الخطأ المطبعي لا يُكتشف إلا حين يعجز العميل عن الدفع. | مؤجل بقرار المالك: سيُنفَّذ داخل صفحة إعدادات البائع في الواجهة، لا بتعديل `seller.ts` يدوياً. |
| 20 Sep 2026 | 2. تعليق قديم | تعليق `seller.ts:16` يحيل إلى دالة اسمها `assertSellerReady` لا وجود لها؛ الفحص الحقيقي هو `sellerGaps()` مستعملة مباشرة. السلوك صحيح، واللافتة فقط خاطئة. | P3 | البحث عن `assertSellerReady` لا يعطي إلا التعليق نفسه. | يُصحَّح ضمن جلسة بناء إعدادات البائع. |
| 20 Sep 2026 | 2. ما طلبه المالك | ملفات PDF محفوظة أصلاً ومجمّدة وقت الإصدار (`pdf_key`)، وسنة الفاتورة مخزّنة أصلاً (`number_year`). الناقص: فلتر السنة في القائمة، وزر الأرشفة، وصفحة إعدادات البائع. Stripe والاشتراكات مؤجلة لمشروع منفصل. | — | `InvoiceQuerySchema` يفلتر بـ`settlement` و`search` و`limit` فقط. | جلسة بناء منفصلة؛ هذه الجلسة تبقى للمراجعة. |
