node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json


#Ignore cursor AI rules
.cursor/rules/codacy.mdc

# Personal photo drops, never part of the site
my images/

# Wrangler's local secret file for `wrangler dev`. Same contents as `.env`.
.dev.vars

# Vercel CLI link data. `.env` and `*.local` above already cover its env files.
.vercel

# Scratch prompts and the Codex helper's local state. Working notes for a
# session, not part of the site.
.codex/
PROMPT.md
REVIEW-PROMPT.md
